---
'@react-native-youtube-bridge/web': patch
---

Publish bundled TypeScript declarations and expose their entry point so consumers can resolve the web player's types from the installed package, including projects that check declaration files and side-effect imports strictly.
