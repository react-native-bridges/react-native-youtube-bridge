# 1.x 개요

1.x는 `YoutubePlayer` 컴포넌트와 callback props 중심의 API였습니다.

```tsx
import { YoutubePlayer } from 'react-native-youtube-bridge';

function App() {
  return <YoutubePlayer source="AbZH7XWDW_k" />;
}
```

새 작업에는 2.x Hook 기반 API를 권장합니다.
